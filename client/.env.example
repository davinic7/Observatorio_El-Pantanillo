# Frontend — Observatorio Industrial
# Copiar este archivo a `.env` y completar con la URL real del backend.
# Vite embebe estas variables en el bundle durante el build.

# URL base del backend (sin slash final).
# Local: http://localhost:8000
# Producción: la URL pública de tu API (ej. https://obs-api.onrender.com)
VITE_API_URL=http://localhost:8000
